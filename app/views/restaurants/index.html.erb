<h1 class="mb-3">All Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to restaurant.name, restaurant_path(restaurant) %>
      — <%= restaurant.category %>
    </li>
  <% end %>
</ul>

<p class="mt-3"><%= link_to "Add a new restaurant", new_restaurant_path, class: "btn btn-primary" %></p>
